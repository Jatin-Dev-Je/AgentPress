{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "root_dir": { "type": "string", "description": "Absolute path to the sandbox root." },
    "max_bytes": { "type": "integer", "minimum": 1, "default": 200000, "description": "Maximum bytes to read from a file." }
  },
  "required": ["root_dir"],
  "additionalProperties": false
}
